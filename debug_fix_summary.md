# 烧录卡死与 AO 抖动问题修改总结

## 背景现象

- 程序烧录后出现卡死或像卡死一样无响应。
- 光敏 AO 数值跳动太快，OLED 页面频繁刷新。
- 串口、协议回复、ADC 采样里存在等待硬件标志位的 `while` 循环，外设异常时可能一直停住。

## 初步判断

这几次问题主要不是编译错误，而是运行时阻塞和刷新过于频繁：

- ADC 校准、ADC 采样如果标志位不上来，原代码会一直等待。
- 串口发送如果发送完成标志异常，`printf` 或协议回复可能一直等待。
- AO 原始值每次轻微变化都会递增版本号，UI 检测到版本变化后刷新 OLED，导致 I2C 刷屏压力很大。
- AO 抖动会同时影响显示刷新和亮暗判断，用户看到数值跳得很快。

## 已修改文件

### `User/light_sensor.c`

目的：避免 ADC 异常时卡死，并让 AO 读取更稳定。

主要改动：

- 增加 `LIGHT_ADC_TIMEOUT`，给 ADC 校准和采样等待加超时。
- 增加 `s_adc_ready`，记录 ADC 是否初始化成功。
- 增加 `s_last_ao_value`，采样失败时返回上一次有效 AO，而不是直接返回 0。
- 每次启动 ADC 转换前调用 `ADC_ClearFlag(ADC1, ADC_FLAG_EOC)`，避免旧的 EOC 标志影响本次采样。

效果：

- ADC 校准失败、采样超时不会让主程序一直卡在 `while`。
- AO 异常采样时不会突然跳到 0。

### `SYSTEM/usart/usart.c`、`SYSTEM/usart/uart_tx.c`

目的：避免 `printf` 串口发送时卡死，并避免调试输出挤占协议响应。

主要改动：

- 新增 TX 环形缓冲，由 USART1 TXE 中断逐字节发送。
- `fputc()` 不再轮询等待硬件标志，只尝试把调试字符放入 TX 队列。
- 调试输出会为二进制协议保留发送空间，队列满时允许丢弃调试字符。

效果：

- `printf` 不会因为串口发送标志异常而一直卡住主循环。
- 大量调试输出不应截断二进制协议响应帧。

### `User/app_protocol_practice.c`

目的：避免二进制协议回复时卡死，并避免发送半帧。

主要改动：

- 协议层先在本地数组中组好完整帧。
- 完整帧一次性写入 TX 环形缓冲。
- TX 队列空间不足时整帧拒绝，不发送残缺协议帧。

效果：

- 协议回复不再轮询等待 USART 发送标志。
- 空间不足时不会在线上留下半个二进制协议帧。

### `User/app_light.c`

目的：降低 AO 抖动，减少 UI 频繁刷新。

主要改动：

- 将光敏采样周期从 `50ms` 调整为 `200ms`。
- 增加 `s_light_filtered_value`，对 AO 做一阶平滑：

```c
filtered = (old * 3 + raw + 2) / 4;
```

- 增加 `LIGHT_AO_REPORT_DELTA = 20`，AO 平滑值变化小于 20 时不更新显示用 AO，也不递增版本号。
- 亮暗判断改用平滑后的 AO，而不是直接用原始 AO。

效果：

- AO 显示不会跟着每次小抖动刷新。
- 亮暗状态判断更稳。
- OLED 刷新压力下降。

### `User/app_ui.c`

目的：限制 OLED 数据刷新频率，避免 I2C 刷屏占满主循环。

主要改动：

- 增加 `UI_DATA_REFRESH_MS = 250`。
- UI 数据版本检查不再每轮主循环都执行，而是最多每 `250ms` 执行一次。
- 页面切换时仍然立即刷新，避免切页后显示不更新。

效果：

- AO 抖动不会触发 OLED 连续刷屏。
- 主循环能留出更多时间处理按键、串口、温度任务。

## 当前仍需注意的问题

1. 本地没有找到 Keil/ARM 命令行编译工具，所以这些修改还没有在这里重新生成 `.hex` 验证。
2. 需要在 Keil 中重新 Build，确认没有编译错误后再烧录。
3. 如果烧录后仍然卡死，下一步优先检查：
   - OLED/I2C 连接是否可靠，SCL/SDA 是否有上拉。
   - 光敏 AO 是否接到 PA0，电压是否在 0 到 3.3V 范围内。
   - 板子外部晶振是否为 8MHz。当前库里 `HSE_VALUE` 是 8MHz；如果硬件是 12MHz，系统时钟配置会不对。
   - ST-Link 调试时停在哪个函数，如果停在 `HardFault_Handler`，需要继续查内存越界或时钟配置。

## 建议验证步骤

1. Keil 中 Rebuild 工程。
2. 烧录后先只看 OLED 是否正常进入菜单。
3. 观察 AO 是否从快速乱跳变成较慢、较平滑变化。
4. 打开串口助手，确认 `printf` 欢迎信息和命令交互是否正常。
5. 如果再次卡死，用调试器暂停，看 PC 当前停在哪个函数。
