# 嵌入式 C 字符串处理知识点

> 以 STM32F103 串口命令行项目为例，总结裸机 C 语言中常用的字符串处理技巧。

---

## 一、字符串逐个字符遍历

### 1.1 基本模式

```c
while (*str != '\0')
{
    // 对 *str 做操作
    str++;               // 指针后移
}
```

C 字符串以 `\0`（空字符，ASCII 值 0）结尾。这个循环会依次访问每个字符，直到遇 `\0` 停止。

### 1.2 为什么不用数组下标

```c
for (int i = 0; i < strlen(str); i++)  // 不推荐：strlen 本身也要遍历一次，效率低
for (int i = 0; str[i] != '\0'; i++)   // 可以，但指针版本更简洁
```

---

## 二、字符串比较：StrEqual

### 2.1 核心思路

两个字符串**同时遍历**，比较每一个字符。只有两个指针**同时到达 `\0`** 才算相等。

### 2.2 关键细节

| 情况 | 结果 | 原因 |
|------|------|------|
| `"ABC"` vs `"ABC"` | 相等 | 三个字符相同，同时到 `\0` |
| `"ABC"` vs `"ABD"` | 不等 | 第三个字符不同 |
| `"ABC"` vs `"ABCD"` | 不等 | 一个先到 `\0`，另一个还有字符 |
| `"ABC"` vs `"AB"` | 不等 | 同上 |

实现要点：**必须比较 `\0` 的位置**，不能只比字符内容。

---

## 三、前缀匹配：StrStartsWith

### 3.1 与 StrEqual 的区别

`StrEqual` 要求**完全相同**。`StrStartsWith` 只要**前几个字符匹配**即可。

### 3.2 实现要点

```c
while (*prefix != '\0')      // 只关心 prefix 是否走完
{
    if (*text != *prefix)
        return 0;
    text++; prefix++;
}
return 1;                    // prefix 走完了就是匹配成功
```

**不检查 text 后面还有什么**——这就是"前缀匹配"的含义。

### 3.3 典型用途

解析带参数的命令。比如 `"LED RED"` 中，`"LED "` 是前缀，`"RED"` 是参数。匹配前缀后，用 `text + strlen(prefix)` 把参数传给子处理函数。

---

## 四、大小写转换：ToUpperString

### 4.1 为什么需要

用户输入不可控：`help`、`Help`、`HELP` 都应识别为同一命令。接收后全部转大写，后续只比较一种形式即可。

### 4.2 ASCII 原理

| 字符 | ASCII |
|------|-------|
| `'A'` ~ `'Z'` | 65 ~ 90 |
| `'a'` ~ `'z'` | 97 ~ 122 |

小写转大写：`ch - 'a' + 'A'`，即 `ch - 32`。

### 4.3 实现

```c
if ((*text >= 'a') && (*text <= 'z'))   // 只处理小写字母
    *text = (char)(*text - 'a' + 'A');   // 映射到大写
```

数字和符号不改动。**直接在原字符串上改**（in-place）。

---

## 五、空格跳过：SkipSpaces

### 5.1 为什么需要

用户输入可能有多余空格：`"  TH   2000  "`。解析参数前必须跳过空白，定位到有效内容。

### 5.2 实现

```c
while ((*text == ' ') || (*text == '\t'))   // 空格和 Tab
    text++;
return text;   // 返回新指针，不修改原字符串
```

### 5.3 设计选择

- 返回新指针而不是修改原字符串 → 调用者仍保留原始字符串
- 不跳过 `\n`、`\r` 等其他空白 → 因为这些在串口接收阶段已被处理

---

## 六、字符串转数字：ParseNumber

### 6.1 核心算法

以 `"2000"` 为例：

| 步骤 | 读到字符 | 运算 | result |
|------|----------|------|--------|
| 初始 | — | — | 0 |
| 第1位 | `'2'` | `0×10 + 2` | 2 |
| 第2位 | `'0'` | `2×10 + 0` | 20 |
| 第3位 | `'0'` | `20×10 + 0` | 200 |
| 第4位 | `'0'` | `200×10 + 0` | 2000 |

实现：`result = result * 10 + (*text - '0')`。

`*text - '0'` 是把字符数字转成整数值。ASCII 中 `'0'`=48, `'1'`=49，所以 `'2' - '0' = 2`。

### 6.2 错误处理

一个严谨的解析函数要检查三种情况：

1. **空输入**：一个数字都没有 → 失败
2. **溢出**：结果超过 `uint16_t` 范围（65535） → 失败
3. **数字后有杂字符**：如 `"123ABC"` → 失败（只接受纯数字）

### 6.3 用 `uint32_t` 做中间变量

即使最终要 `uint16_t`，中间计算也用 `uint32_t`：

```c
uint32_t result = 0;    // 用 32 位累加
if (result > 65535u)    // 在赋值前检查范围
    return 0;
*value = (uint16_t)result;  // 通过检查后才转 16 位
```

如果直接用 `uint16_t`，`result * 10` 可能先溢出再被截断，溢出后的值可能刚好 ≤ 65535，绕过检查。

---

## 七、整数表示小数的技巧

### 7.1 问题

温度传感器返回的是 ×10 的整数（如 `256` 表示 25.6°C）。嵌入式里不用 `float` 更省资源。

### 7.2 打印方法

```c
printf("%d.%d", temp10 / 10, temp10 % 10);  // 整数商.余数
```

| temp10 | `/ 10`（商） | `% 10`（余数） | 输出 |
|--------|-------------|---------------|------|
| 256 | 25 | 6 | `25.6` |
| 0 | 0 | 0 | `0.0` |
| -15 | -1 | -5 | 需特殊处理 |

### 7.3 负数处理

C 语言中 `-15 / 10` 的结果取决于编译器（可能是 `-1` 或 `-2`）。最稳妥的做法是：

```c
if (temp10 < 0)
{
    printf("-");                     // 先打负号
    temp10 = (int16_t)(0 - temp10);  // 转正
}
printf("%d.%d", temp10 / 10, temp10 % 10);  // 正数除法，结果确定
```

---

## 八、static 关键字在 C 文件中的作用

所有工具函数都加了 `static`：

```c
static int StrEqual(const char *a, const char *b) { ... }
```

`static` 在这里的意思是：**函数只在本 `.c` 文件内可见**（内部链接）。

### 8.1 为什么加

1. **防止命名冲突**：其他 `.c` 文件可能也有 `StrEqual`，加了 `static` 互不干扰
2. **隐藏实现**：外部调用者不需要知道这些辅助函数的存在
3. **方便编译器优化**：编译器知道函数不会被外部调用，可能直接内联

### 8.2 什么时候不加 `static`

只有被其他 `.c` 文件引用的函数才不加 `static`（比如 `App_UARTPractice_Init` 和 `App_UARTPractice_Task`），它们要在 `.h` 文件里声明。

---

## 九、总结：命令解析的通用模板

写一个串口命令行，只需要这几种函数：

| 能力 | 对应函数 | 作用 |
|------|----------|------|
| 字符串比较 | `StrEqual` | 匹配无参数命令（HELP, PING） |
| 前缀匹配 | `StrStartsWith` | 匹配有参数命令（LED xxx, TH xxx） |
| 大小写归一 | `ToUpperString` | 让命令不区分大小写 |
| 空白跳过 | `SkipSpaces` | 处理多余空格 |
| 字符串转数字 | `ParseNumber` | 解析命令中的数字参数 |

这些技术不只适用于 STM32，任何 C 语言的命令行解析场景都通用。
